<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>OCR Scanner App Mockup</title>
  <style>
    body {
      font-family: 'San Francisco', Arial, sans-serif;
      background: #23272e;
      margin: 0;
      padding: 0;
      color: #fff;
    }
    .window {
      width: 1400px;
      height: 900px;
      margin: 40px auto;
      background: #2c2f36;
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.38);
      display: flex;
      flex-direction: column;
      overflow: hidden;
      position: relative;
    }
    .top-bar {
      display: flex;
      align-items: center;
      padding: 0 24px;
      height: 60px;
      background: #181a1b;
      border-bottom: 1px solid #333;
      gap: 16px;
    }
    .top-bar input[type="text"] {
      width: 160px;
      padding: 6px 10px;
      border-radius: 6px;
      border: 1px solid #444;
      font-size: 16px;
      background: #23272e;
      color: #fff;
    }
    .top-bar select, .top-bar button {
      padding: 7px 16px;
      border-radius: 6px;
      border: 1px solid #444;
      background: #23272e;
      font-size: 16px;
      color: #fff;
      cursor: pointer;
    }
    .top-bar .start-btn {
      background: #4caf50;
      color: #fff;
      border: none;
      font-weight: bold;
    }
    .top-bar .settings-btn {
      background: #23272e;
      color: #90caf9;
      border: 1px solid #444;
      border-radius: 6px;
      font-size: 16px;
      font-weight: bold;
      padding: 7px 16px;
      cursor: pointer;
    }
    .dropdown {
      position: relative;
      display: inline-block;
    }
    .dropdown-content {
      display: none;
      position: absolute;
      background: #23272e;
      min-width: 140px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.14);
      z-index: 10;
      right: 0;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid #444;
    }
    .dropdown:hover .dropdown-content {
      display: block;
    }
    .dropdown-content button, .confidence-dropdown-content li {
      width: 100%;
      padding: 10px 16px;
      background: none;
      border: none;
      text-align: left;
      cursor: pointer;
      font-size: 15px;
      color: #fff;
      list-style: none;
    }
    .dropdown-content button:hover, .confidence-dropdown-content li:hover {
      background: #333;
    }
    .file-list-dropdown {
      background: #23272e;
      border-bottom: 1px solid #333;
      padding: 0 24px;
      height: 80px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      transition: height 0.2s;
      color: #fff;
    }
    .file-list-header {
      display: flex;
      align-items: center;
      gap: 32px;
      font-size: 17px;
      position: relative;
    }
    .confidence-label {
      position: relative;
      display: inline-block;
    }
    .confidence-label .confidence-dropdown-content {
      display: none;
      position: absolute;
      left: 0;
      top: 28px;
      background: #23272e;
      min-width: 180px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.14);
      z-index: 20;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid #444;
      padding: 0;
    }
    .confidence-label:hover .confidence-dropdown-content {
      display: block;
    }
    .confidence-dropdown-content li {
      padding: 10px 16px;
      font-size: 15px;
      color: #fff;
      background: none;
      border: none;
      text-align: left;
      cursor: pointer;
      list-style: none;
    }
    .confidence-dropdown-content li:hover {
      background: #333;
    }
    .file-list-header .low {
      color: #fff;
      background: #e53935;
      border-radius: 12px;
      padding: 2px 12px;
      font-weight: bold;
    }
    .file-list-header .high {
      color: #fff;
      background: #43a047;
      border-radius: 12px;
      padding: 2px 12px;
      font-weight: bold;
    }
    .file-list-header .progress-bar {
      height: 12px;
      background: #333;
      border-radius: 6px;
      width: 180px;
      margin-left: 18px;
      overflow: hidden;
      position: relative;
      display: flex;
      align-items: center;
    }
    .file-list-header .progress-bar .progress {
      height: 100%;
      background: #43a047;
      width: 60%; /* Example: 60% done */
      border-radius: 6px;
      transition: width 0.3s;
    }
    .file-list-header .progress-bar .progress-label {
      position: absolute;
      right: 8px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 13px;
      color: #fff;
      font-weight: bold;
      z-index: 2;
      text-shadow: 0 1px 2px #222;
    }
    .main-panel {
      flex: 1;
      display: flex;
      min-height: 0;
      position: relative;
    }
    .left-panel {
      width: 600px;
      background: #181a1b;
      border-right: 1px solid #333;
      overflow-y: auto;
      padding: 24px 0 24px 24px;
      display: flex;
      flex-direction: column;
      gap: 24px;
      resize: horizontal;
      min-width: 300px;
      max-width: 900px;
    }
    .divider {
      width: 8px;
      background: #444;
      cursor: ew-resize;
      position: relative;
      z-index: 2;
    }
    .right-panel {
      flex: 1;
      background: #181a1b;
      overflow-y: auto;
      padding: 24px 24px 24px 0;
      display: flex;
      flex-direction: column;
      align-items: stretch;
      gap: 24px;
      min-width: 320px;
      max-width: 900px;
      resize: horizontal;
      position: relative;
    }
    .files-preview-header {
      text-align: center;
      font-size: 19px;
      font-weight: bold;
      margin-bottom: 18px;
    }
    .file-preview {
      background: #23272e;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.14);
      padding: 12px;
      min-height: 220px;
      height: 320px;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: stretch;
      border: 1.5px solid #bdbdbd;
      margin-bottom: 12px;
    }
    .file-preview.high-confidence {
      border: 2px solid #43a047;
      background: #202923;
    }
    .file-preview.low-confidence {
      border: 2px solid #e53935;
      background: #231f1f;
    }
    .file-preview .filename {
      font-size: 16px;
      font-weight: bold;
      margin-bottom: 8px;
      color: #e53935;
    }
    .file-preview .img-mock {
      flex: 1 1 auto;
      width: 100%;
      background: #333;
      border-radius: 6px;
      margin-bottom: 6px;
      display: flex;
      flex-direction: column;
      align-items: stretch;
      justify-content: flex-start;
      color: #bbb;
      font-size: 15px;
      overflow-y: auto;
      position: relative;
    }
    .file-preview .img-mock .page {
      flex: 1 1 auto;
      width: 100%;
      min-height: 0;
      background: #222;
      border-radius: 4px;
      margin-bottom: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #bbb;
      font-size: 14px;
    }
    .file-preview .page-controls {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      margin-top: 2px;
    }
    .file-preview .page-controls button {
      padding: 2px 8px;
      border-radius: 4px;
      border: 1px solid #444;
      background: #23272e;
      color: #fff;
      font-size: 13px;
      cursor: pointer;
      transition: background 0.2s;
    }
    .file-preview .page-controls .page-num {
      font-size: 13px;
      color: #90caf9;
      min-width: 36px;
      text-align: center;
    }
    .right-panel .action-bar {
      position: static;
      display: flex;
      flex-direction: row;
      justify-content: flex-end;
      align-items: center;
      gap: 16px;
      margin-bottom: 10px;
      background: none;
      box-shadow: none;
      border: none;
      padding: 0;
    }
    .right-panel .action-bar button {
      padding: 7px 16px;
      border-radius: 6px;
      border: 1px solid #444;
      background: #23272e;
      font-size: 16px;
      color: #fff;
      cursor: pointer;
      font-weight: bold;
      transition: background 0.2s, color 0.2s;
    }
    .right-panel .action-bar .save-btn {
      background: #1976d2;
      color: #fff;
      border: none;
    }
    .right-panel .action-bar .ai-btn {
      background: #23272e;
      color: #90caf9;
      border: 1px solid #444;
      border-radius: 6px;
      font-size: 16px;
      font-weight: bold;
      padding: 7px 16px;
      cursor: pointer;
      margin-left: 12px;
      transition: background 0.2s, color 0.2s;
    }
    .right-panel .action-bar .knowledge-btn {
      background: #23272e;
      color: #ffeb3b;
      border: 1px solid #444;
      border-radius: 6px;
      font-size: 16px;
      font-weight: bold;
      padding: 7px 16px;
      cursor: pointer;
      margin-left: 12px;
      transition: background 0.2s, color 0.2s;
    }
    .summary-box {
      background: #23272e;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.14);
      padding: 16px 18px;
      border: 1.5px solid #bdbdbd;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      direction: rtl;
      text-align: right;
      font-family: 'Arial', 'Noto Sans Hebrew', sans-serif;
      font-size: 17px;
      flex: 1 1 auto;
      min-height: 320px;
      max-width: 900px;
      width: 90%;
      margin-left: auto;
      margin-right: auto;
      box-sizing: border-box;
      position: relative;
      color: #fff;
    }
    .summary-box .history {
      width: 100%;
      margin-bottom: 12px;
      display: flex;
      flex-direction: column;
      gap: 18px;
    }
    .summary-box .history-entry {
      background: #181a1b;
      border-radius: 7px;
      padding: 8px 12px;
      margin-bottom: 0;
      font-size: 15px;
      color: #bdbdbd;
    }
    .summary-box .current-summary-label {
      text-align: center;
      font-size: 19px;
      font-weight: bold;
      margin-bottom: 6px;
    }
    .summary-box .current-summary {
      background: #333;
      border-radius: 7px;
      padding: 10px 14px;
      font-size: 17px;
      color: #fff;
      margin-bottom: 0;
      font-weight: bold;
    }
    .progress-bar {
      height: 8px;
      background: #333;
      border-radius: 4px;
      width: 220px;
      margin-left: 18px;
      overflow: hidden;
      position: relative;
    }
    .progress-bar .progress {
      height: 100%;
      background: #43a047;
      width: 60%; /* Example: 60% done */
      border-radius: 4px;
      transition: width 0.3s;
    }
  </style>
</head>
<body>
  <div class="window">
    <div class="top-bar">
      <span>Patient ID:</span>
      <input type="text" value="123456" style="border-color:#e53935; color:#e53935; background:#23272e;" />
      <button>Prompt Template ▼</button>
      <div class="dropdown">
        <button>Export ▼</button>
        <div class="dropdown-content">
          <button>Export as PDF</button>
          <button>Export as TXT</button>
          <button>Export as DOCX</button>
        </div>
      </div>
      <div class="dropdown">
        <button>OCR Engine ▼</button>
        <div class="dropdown-content">
          <button>Google Vision</button>
          <button>ABBYY</button>
        </div>
      </div>
      <button class="settings-btn">Settings</button>
      <button class="knowledge-btn">Knowledge Files ▼</button>
      <button>Select Files/Folders</button>
      <button class="start-btn">Start</button>
    </div>
    <div class="file-list-dropdown">
      <div class="file-list-header">
        <span class="confidence-label low">Low-confidence files: 3
          <ul class="confidence-dropdown-content">
            <li>file2.jpg (68%)</li>
            <li>file5.pdf (72%)</li>
            <li>file8.tiff (80%)</li>
          </ul>
        </span>
        <span class="confidence-label high">High-confidence files: 7
          <ul class="confidence-dropdown-content">
            <li>file1.pdf (98%)</li>
            <li>file3.jpg (95%)</li>
            <li>file4.tiff (91%)</li>
            <li>file6.pdf (89%)</li>
            <li>file7.jpg (87%)</li>
            <li>file9.pdf (85%)</li>
            <li>file10.tiff (83%)</li>
          </ul>
        </span>
        <div class="progress-bar">
          <div class="progress" style="width:60%"></div>
          <span class="progress-label">6/10 (60%)</span>
        </div>
      </div>
      <button class="minimize-btn">▲</button>
    </div>
    <div class="main-panel" id="main-panel">
      <div class="left-panel" id="left-panel">
        <div class="files-preview-header" style="text-align:center; font-size:19px; font-weight:bold; margin-bottom:18px;">Files Preview</div>
        <!-- High-confidence files -->
        <div class="file-preview high-confidence">
          <div class="filename">file1.pdf (98%)</div>
          <div class="img-mock">
            <div class="page">[Page 1 Preview]</div>
            <div class="page">[Page 2 Preview]</div>
            <div class="page">[Page 3 Preview]</div>
            <div class="page-controls">
              <button>&lt; Prev</button>
              <span class="page-num">1 / 3</span>
              <button>Next &gt;</button>
            </div>
          </div>
        </div>
        <div class="file-preview high-confidence">
          <div class="filename">file3.jpg (95%)</div>
          <div class="img-mock">[PDF/Image Preview]</div>
        </div>
        <div class="file-preview high-confidence">
          <div class="filename">file4.tiff (91%)</div>
          <div class="img-mock">[PDF/Image Preview]</div>
        </div>
        <!-- Low-confidence files -->
        <div class="file-preview low-confidence">
          <div class="filename">file2.jpg (68%)</div>
          <div class="img-mock">[PDF/Image Preview]</div>
        </div>
        <div class="file-preview low-confidence">
          <div class="filename">file5.pdf (72%)</div>
          <div class="img-mock">[PDF/Image Preview]</div>
        </div>
        <div class="file-preview low-confidence">
          <div class="filename">file8.tiff (80%)</div>
          <div class="img-mock">[PDF/Image Preview]</div>
        </div>
        <div style="text-align:center; color:#bbb; font-size:13px; margin-top:18px;">(Files can be ordered by date found in OCR text, not file creation date)</div>
      </div>
      <div class="divider" id="divider"></div>
      <div class="right-panel">
        <div class="action-bar">
          <button class="save-btn">Save All Corrections</button>
          <button class="ai-btn">AI Assist 🤖</button>
        </div>
        <div class="current-summary-label" style="text-align:center; font-size:19px; font-weight:bold; margin-bottom:10px;">סיכום נוכחי של המטופל</div>
        <div class="summary-box">
          <div class="history">
            <div class="history-entry">סיכום היסטורי 1 של המטופל (הכי ישן)</div>
            <div class="history-entry">סיכום היסטורי 2 של המטופל</div>
            <div class="history-entry">סיכום היסטורי 3 של המטופל</div>
          </div>
          <div class="current-summary"></div>
        </div>
      </div>
    </div>
    <script>
      // Resizable divider logic
      const divider = document.getElementById('divider');
      const leftPanel = document.getElementById('left-panel');
      const mainPanel = document.getElementById('main-panel');
      let isResizing = false;

      divider.addEventListener('mousedown', function(e) {
        isResizing = true;
        document.body.style.cursor = 'ew-resize';
      });

      document.addEventListener('mousemove', function(e) {
        if (!isResizing) return;
        const minWidth = 300;
        const maxWidth = 900;
        const rect = mainPanel.getBoundingClientRect();
        let newWidth = e.clientX - rect.left;
        if (newWidth < minWidth) newWidth = minWidth;
        if (newWidth > maxWidth) newWidth = maxWidth;
        leftPanel.style.width = newWidth + 'px';
      });

      document.addEventListener('mouseup', function(e) {
        isResizing = false;
        document.body.style.cursor = '';
      });
    </script>
  </div>
</body>
</html>
